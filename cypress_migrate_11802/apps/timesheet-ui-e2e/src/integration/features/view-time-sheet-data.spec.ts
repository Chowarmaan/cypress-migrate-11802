// import { getGreeting } from '../support/app.po';

describe('View Time Sheet Data', () => {
	beforeEach(() => cy.visit('/#/view'));

	// it('should have the proper title for the page', () => {
	// 	cy.title().should('equal', 'View Time Sheet Data');
	// });

	// it('should show a list of time sheet entries', () => {
	// 	const TimeSheetDataList = () => cy.get('[data-testid="time-sheet-entry-list"]');
	// 	TimeSheetDataList().should('exist');
	// 	TimeSheetDataList().should('be.visible');
	// 	TimeSheetDataList().should('have.length', 3);
	// });
});
